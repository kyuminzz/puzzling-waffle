using UnityEngine;

public class PotraitCard : MonoBehaviour
{

    public int index { get; set; }
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
